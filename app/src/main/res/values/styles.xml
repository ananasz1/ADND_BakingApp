<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="CardStyle">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:layout_marginBottom">@dimen/margin_topbottom</item>
        <item name="android:layout_marginLeft">@dimen/margin_side</item>
        <item name="android:layout_marginRight">@dimen/margin_side</item>
        <item name="android:layout_marginTop">@dimen/margin_topbottom</item>
    </style>

    <style name="RecyclerStyle">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">match_parent</item>
        <item name="android:layout_marginBottom">@dimen/margin_topbottom</item>
        <item name="android:layout_marginTop">@dimen/margin_topbottom</item>
        <item name="android:scrollbars">vertical</item>
    </style>

</resources>